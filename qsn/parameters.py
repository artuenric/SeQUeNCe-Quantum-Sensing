"""
MÓDULO DESCONTINUADO
--------------------

Este arquivo foi substituído por:
 - config.py (na raiz) para o dicionário CONFIG
 - parameters_utils.py (na raiz) para a função set_parameters

Por favor, atualize suas importações para:
    from config import CONFIG
    from parameters_utils import set_parameters
"""

raise ImportError(
    "qsn.parameters foi descontinuado. Use 'config' e 'parameters_utils' na raiz do projeto."
)